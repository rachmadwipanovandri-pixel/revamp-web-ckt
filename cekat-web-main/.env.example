NEXT_PUBLIC_SITE_URL=https://cekat.ai

# Headless WordPress REST API base (no trailing slash). Blog reads from here.
WORDPRESS_API_URL=https://wordpress-1rwf.onrender.com/wp-json/wp/v2

# Google Apps Script Web App URL (deployed from the contact spreadsheet:
# Extensions > Apps Script > deploy as Web App) that appends each contact
# form submission as a new row.
CONTACT_SHEET_WEBHOOK_URL=
